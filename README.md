Fowl - Document Model and Query Layer for FoundationDB
======================================================


This module allows to store and retrieve javascript objects on
a foundationDB database.

It provides a flexible api based on keypath and CRUD semantics.

